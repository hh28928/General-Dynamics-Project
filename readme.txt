To clean the data and prepare it for R to be analyzed, 

navigate to the directory, 

run the Shell script named (value_strip.sh)
./value_strip.sh

run the Bash script named (value_strip_d2.bash)
./value_strip_d2.bash
